https://git-scm.com/downloads

/// first time from new folder
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/Aakashdeveloper/testgitmrng.git
git push -u origin master

git status

git pull

// next all time
git add .
git commit -m "first commit"
git push

git branch

// new branch
git checkout -b listing

//change branch
git checkout master